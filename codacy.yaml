runtimes:
  - node@20

tools:
  - eslint@9.33.0
  - trivy

tool_configurations:
  trivy: .codacy/tools-configs/trivy.yaml

# Ignore auto-generated lockfile from Lizard/complexity checks
exclude_paths:
  - package-lock.json
